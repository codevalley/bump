# Bump Service Configuration

# Matching parameters
BUMP_MAX_DISTANCE_METERS=5.0
BUMP_MAX_TIME_DIFF_MS=1500  # Increased to handle server-assigned timestamp differences
BUMP_DEFAULT_TTL_MS=500

# Scoring weights (must sum to 1.0)
BUMP_TEMPORAL_WEIGHT=0.7
BUMP_SPATIAL_WEIGHT=0.3

# Maintenance
BUMP_CLEANUP_INTERVAL_MS=1000

# Physical constants
BUMP_EARTH_RADIUS_METERS=6371000.0
